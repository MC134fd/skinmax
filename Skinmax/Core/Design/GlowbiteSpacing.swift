import Foundation

enum GlowbiteSpacing {
    // MARK: - 8pt Grid
    static let xs: CGFloat = 4
    static let sm: CGFloat = 8
    static let md: CGFloat = 16
    static let lg: CGFloat = 24
    static let xl: CGFloat = 32
    static let xxl: CGFloat = 48

    // MARK: - Screen
    static let screenPadding: CGFloat = 20

    // MARK: - Cards
    static let cardPadding: CGFloat = 16
    static let cardPaddingSmall: CGFloat = 14
    static let cardPaddingLarge: CGFloat = 20
    static let cardCornerRadius: CGFloat = 20
    static let cardCornerRadiusSmall: CGFloat = 16
    static let cardCornerRadiusLarge: CGFloat = 28

    // MARK: - Buttons
    static let buttonCornerRadius: CGFloat = 16
    static let pillCornerRadius: CGFloat = 100
    static let inputFieldRadius: CGFloat = 14

    // MARK: - Tags / Chips
    static let tagCornerRadius: CGFloat = 100

    // MARK: - Metric Grid
    static let metricGridSpacing: CGFloat = 10

    // MARK: - Tab Bar
    static let tabBarCornerRadius: CGFloat = 22
    static let tabBarInset: CGFloat = 10
}
